*** Project uder development ! 
We are currently changing the UI of the whole project so that you can easily use our product

# Customized-Chatbot


run command 

pip install -r requirements.txt
uvicorn API:app --reload
